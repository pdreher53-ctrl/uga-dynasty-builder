import { CodeTypingChallenge } from '../components/exercises/CodeTyping';
import { DragDropChallenge } from '../components/exercises/DragDropMatch';
import { FillBlankChallenge } from '../components/exercises/FillInBlank';
import { DebugChallengeData } from '../components/exercises/DebugChallenge';

// ═══════════════════════════════════════════════════════════════
// INTERACTIVE CHALLENGES — Duolingo-style exercises
// ═══════════════════════════════════════════════════════════════

export const codeTypingChallenges: CodeTypingChallenge[] = [
  {
    type: 'code-typing',
    id: 'ct-001',
    prompt: 'Write a SQL query to select all recruits from Georgia, ordered by star rating.',
    expectedCode: "SELECT * FROM recruits WHERE state = 'Georgia' ORDER BY stars DESC;",
    hint: 'Start with SELECT *, add a WHERE for state, and ORDER BY stars.',
    xpReward: 75,
    skill: 'sql',
  },
  {
    type: 'code-typing',
    id: 'ct-002',
    prompt: 'Create a Python for loop that prints numbers 1 through 5.',
    expectedCode: 'for i in range(1, 6):\n    print(i)',
    hint: 'Use range(1, 6) — range is exclusive on the upper bound.',
    xpReward: 75,
    skill: 'python',
  },
  {
    type: 'code-typing',
    id: 'ct-003',
    prompt: 'Write a JavaScript arrow function that doubles a number.',
    expectedCode: 'const double = (n) => n * 2;',
    hint: 'Arrow functions use => syntax. No curly braces needed for one-liners.',
    xpReward: 75,
    skill: 'javascript',
  },
  {
    type: 'code-typing',
    id: 'ct-004',
    prompt: 'Write a React component that renders "Go Dawgs!" in an h1 tag.',
    expectedCode: 'function GoDawgs() {\n  return <h1>Go Dawgs!</h1>;\n}',
    hint: 'Define a function that returns JSX with an h1 element.',
    xpReward: 75,
    skill: 'react',
  },
  {
    type: 'code-typing',
    id: 'ct-005',
    prompt: 'Write a fetch call to get data from "/api/recruits".',
    expectedCode: "const response = await fetch('/api/recruits');",
    hint: 'Use await with fetch() and a string URL.',
    xpReward: 75,
    skill: 'apis',
  },
  {
    type: 'code-typing',
    id: 'ct-006',
    prompt: 'Write a git command to create and switch to a new branch called "feature-recruit-board".',
    expectedCode: 'git checkout -b feature-recruit-board',
    hint: 'Use git checkout with the -b flag to create and switch.',
    xpReward: 50,
    skill: 'git',
  },
];

export const matchChallenges: DragDropChallenge[] = [
  {
    type: 'match',
    id: 'match-001',
    prompt: 'Match each SQL keyword to what it does — like matching recruits to their positions!',
    pairs: [
      { term: 'SELECT', definition: 'Retrieve data' },
      { term: 'WHERE', definition: 'Filter rows' },
      { term: 'ORDER BY', definition: 'Sort results' },
      { term: 'JOIN', definition: 'Combine tables' },
    ],
    xpReward: 60,
    skill: 'sql',
  },
  {
    type: 'match',
    id: 'match-002',
    prompt: 'Match each HTTP method to its action — like play calls in the huddle!',
    pairs: [
      { term: 'GET', definition: 'Read data' },
      { term: 'POST', definition: 'Create new' },
      { term: 'PUT', definition: 'Update existing' },
      { term: 'DELETE', definition: 'Remove data' },
    ],
    xpReward: 60,
    skill: 'apis',
  },
  {
    type: 'match',
    id: 'match-003',
    prompt: 'Match each Python type to its example — scout these data types!',
    pairs: [
      { term: 'str', definition: '"Go Dawgs"' },
      { term: 'int', definition: '42' },
      { term: 'list', definition: '[1, 2, 3]' },
      { term: 'dict', definition: '{"key": "value"}' },
    ],
    xpReward: 60,
    skill: 'python',
  },
  {
    type: 'match',
    id: 'match-004',
    prompt: 'Match each React concept to its purpose!',
    pairs: [
      { term: 'useState', definition: 'Manage state' },
      { term: 'useEffect', definition: 'Side effects' },
      { term: 'props', definition: 'Pass data down' },
      { term: 'JSX', definition: 'Template syntax' },
    ],
    xpReward: 60,
    skill: 'react',
  },
  {
    type: 'match',
    id: 'match-005',
    prompt: 'Match each git command to what it does!',
    pairs: [
      { term: 'git add', definition: 'Stage changes' },
      { term: 'git commit', definition: 'Save snapshot' },
      { term: 'git push', definition: 'Upload to remote' },
      { term: 'git pull', definition: 'Download changes' },
    ],
    xpReward: 60,
    skill: 'git',
  },
];

export const fillBlankChallenges: FillBlankChallenge[] = [
  {
    type: 'fill-blank',
    id: 'fb-001',
    prompt: 'Complete the SQL query to count all 5-star recruits.',
    codeBefore: 'SELECT ',
    codeAfter: "(*) FROM recruits WHERE stars = 5;",
    answer: 'COUNT',
    alternatives: ['count'],
    hint: 'Which aggregate function counts rows?',
    xpReward: 50,
    skill: 'sql',
  },
  {
    type: 'fill-blank',
    id: 'fb-002',
    prompt: 'Complete the JavaScript to destructure name from an object.',
    codeBefore: 'const { ',
    codeAfter: ' } = recruit;',
    answer: 'name',
    hint: 'What property are we pulling out of the recruit object?',
    xpReward: 50,
    skill: 'javascript',
  },
  {
    type: 'fill-blank',
    id: 'fb-003',
    prompt: 'Complete the Python list comprehension.',
    codeBefore: 'stars = [r.stars for r in recruits if r.',
    codeAfter: ' >= 4]',
    answer: 'stars',
    hint: 'Filter by the stars attribute.',
    xpReward: 50,
    skill: 'python',
  },
  {
    type: 'fill-blank',
    id: 'fb-004',
    prompt: 'Complete the React hook call to initialize state to 0.',
    codeBefore: 'const [count, setCount] = ',
    codeAfter: '(0);',
    answer: 'useState',
    hint: 'Which React hook manages component state?',
    xpReward: 50,
    skill: 'react',
  },
  {
    type: 'fill-blank',
    id: 'fb-005',
    prompt: 'Complete the API fetch to parse JSON from the response.',
    codeBefore: 'const data = await response.',
    codeAfter: '();',
    answer: 'json',
    hint: 'Call the method that parses the response body as JSON.',
    xpReward: 50,
    skill: 'apis',
  },
  {
    type: 'fill-blank',
    id: 'fb-006',
    prompt: 'Complete the git command to see what files changed.',
    codeBefore: 'git ',
    codeAfter: '',
    answer: 'status',
    alternatives: ['diff'],
    hint: 'Which git command shows the working tree status?',
    xpReward: 50,
    skill: 'git',
  },
  {
    type: 'fill-blank',
    id: 'fb-007',
    prompt: 'Complete the SQL to group results by position.',
    codeBefore: "SELECT position, COUNT(*) FROM recruits ",
    codeAfter: ' position;',
    answer: 'GROUP BY',
    alternatives: ['group by'],
    hint: 'Which SQL clause groups rows together?',
    xpReward: 50,
    skill: 'sql',
  },
  {
    type: 'fill-blank',
    id: 'fb-008',
    prompt: 'Complete the prompt engineering pattern.',
    codeBefore: 'You are a helpful ',
    codeAfter: '. Given the following data, summarize the top 3 trends.',
    answer: 'assistant',
    alternatives: ['analyst', 'expert', 'AI'],
    hint: 'What role are you assigning to the AI?',
    xpReward: 50,
    skill: 'promptEngineering',
  },
];

export const debugChallenges: DebugChallengeData[] = [
  {
    type: 'debug',
    id: 'debug-001',
    prompt: 'This Python function should return the average, but something is wrong. Find the buggy line!',
    buggyCode: 'def average(numbers):\n    total = 0\n    for num in numbers:\n        total += num\n    return total / len(total)',
    fixedCode: 'def average(numbers):\n    total = 0\n    for num in numbers:\n        total += num\n    return total / len(numbers)',
    bugLine: 5,
    explanation: "Line 5 calls len(total) but 'total' is a number, not a list. Should be len(numbers) to get the count of items.",
    xpReward: 100,
    skill: 'python',
  },
  {
    type: 'debug',
    id: 'debug-002',
    prompt: "This SQL query should find 5-star recruits from Georgia, but it's not working. Find the bug!",
    buggyCode: "SELECT name, stars\nFROM recruits\nWHERE state = 'Georgia'\nAND stars = '5'\nORDER BY name;",
    fixedCode: "SELECT name, stars\nFROM recruits\nWHERE state = 'Georgia'\nAND stars = 5\nORDER BY name;",
    bugLine: 4,
    explanation: "Line 4 compares stars to the string '5' instead of the number 5. Stars is an integer column, so don't use quotes.",
    xpReward: 100,
    skill: 'sql',
  },
  {
    type: 'debug',
    id: 'debug-003',
    prompt: 'This JavaScript should filter for 4+ star recruits but returns an empty array. Find the bug!',
    buggyCode: "const topRecruits = recruits.filter(\n  (r) => {\n    r.stars >= 4\n  }\n);",
    fixedCode: "const topRecruits = recruits.filter(\n  (r) => {\n    return r.stars >= 4;\n  }\n);",
    bugLine: 3,
    explanation: "Line 3 is missing a 'return' statement. With curly braces in an arrow function, you must explicitly return. Without curly braces, it's implicit.",
    xpReward: 100,
    skill: 'javascript',
  },
  {
    type: 'debug',
    id: 'debug-004',
    prompt: 'This React component should update the count when clicked, but clicking does nothing. Find the bug!',
    buggyCode: 'function Counter() {\n  const [count, setCount] = useState(0);\n  const handleClick = () => {\n    count = count + 1;\n  };\n  return <button onClick={handleClick}>{count}</button>;\n}',
    fixedCode: 'function Counter() {\n  const [count, setCount] = useState(0);\n  const handleClick = () => {\n    setCount(count + 1);\n  };\n  return <button onClick={handleClick}>{count}</button>;\n}',
    bugLine: 4,
    explanation: "Line 4 directly mutates 'count' instead of using setCount(). In React, state must be updated through the setter function to trigger a re-render.",
    xpReward: 100,
    skill: 'react',
  },
  {
    type: 'debug',
    id: 'debug-005',
    prompt: 'This fetch call should get recruit data but always fails. Find the bug!',
    buggyCode: "async function getRecruits() {\n  const response = fetch('/api/recruits');\n  const data = await response.json();\n  return data;\n}",
    fixedCode: "async function getRecruits() {\n  const response = await fetch('/api/recruits');\n  const data = await response.json();\n  return data;\n}",
    bugLine: 2,
    explanation: "Line 2 is missing 'await' before fetch(). Without await, 'response' is a Promise, not the actual Response object, so .json() fails.",
    xpReward: 100,
    skill: 'apis',
  },
];

// All interactive challenges combined
export const allInteractiveChallenges = [
  ...codeTypingChallenges,
  ...matchChallenges,
  ...fillBlankChallenges,
  ...debugChallenges,
];
